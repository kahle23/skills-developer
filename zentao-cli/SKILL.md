---
name: zentao-cli
description: 通过 zentao 命令行工具查询和操作禅道（ZenTao）数据，覆盖项目集、产品、项目、执行、需求、Bug、任务、测试用例、测试单、产品计划、版本、发布、反馈、工单、应用、用户、附件等模块的增删改查及状态流转。当用户提到禅道、zentao、查询项目进展、获取 Bug 列表、创建任务、更新需求状态等项目管理操作时使用本技能。
license: MIT
metadata:
  author: Sun Hao <sunhao@chandao.com>
  repository: https://github.com/easysoft/zentao-cli.git
  keywords: [zentao, 禅道, cli, project-management]
  version: 0.1.8
---

# 禅道 CLI

通过 `zentao` 命令行工具查询和操作禅道（ZenTao）数据。CLI 基于禅道 RESTful API 2.0，自动处理认证、分页，支持工作区上下文、数据过滤/排序/搜索、Markdown 输出，对 AI Agent 友好。

## 凭证安全（红线）

- **不要在对话里收集账号密码**：用户尚未登录时，引导其在终端执行 `zentao login`，或执行任意 `zentao` 命令触发首次自动登录提示，由用户自行输入凭证。
- **严禁读取本地凭证**：`ZENTAO_PASSWORD` / `ZENTAO_TOKEN` 环境变量、`~/.config/zentao/zentao.json` 配置文件均不可读取。所有禅道数据通过 `zentao` 命令获取，凭证由 CLI 内部处理。

## 文档索引

| 编号 | 主题 | 何时读取 |
|-----|------|---------|
| 01 | 安装与认证（安装/登录/账户切换/退出/配置文件/环境变量） | 首次使用、遇到登录或账户问题 |
| 02 | 命令与模块（命令格式/模块速查/列表范围/帮助） | 不确定模块或操作时 |
| 03 | 数据处理（摘取/过滤/搜索/排序/分页/输出格式/管道） | 需要筛选、搜索、分页或程序化处理数据 |
| 04 | 高级功能（config/version/autocomplete/静默/批量/工作区/MCP） | 配置默认值、批量操作、MCP 集成 |
| 05 | AI 使用策略与意图识别 | 编写 AI 操作禅道的流程、将用户意图映射为命令 |
| 06 | 常用操作示例 | 需要现成命令模板时 |
| 07 | 错误代码与排查 | 命令报错时按错误码定位处理方式 |
| 08 | 注意事项（实战坑点，持续补充） | 涉及执行/任务查询、搜索、状态筛选，或查不到数据时读 |

## 06–08 条目速览（触发按需读取的钩子）

索引标题是"主题"，下面列出关键条目作为触发钩子，命中即读对应文档。

- **06 常用操作示例**：查进行中项目/执行｜搜索已关闭执行中的任务（ID断层探测）｜创建需求并关联计划｜创建并解决 Bug｜创建/启动/完成任务｜查看帮助
- **07 错误代码**：10xx 认证配置（E1001未登录/E1004 Token失效/E1008版本不支持）｜20xx API（E2002对象不存在/E2003缺参数/E2006无权限/E2008服务端错误）｜30xx 数据处理（E3001字段不存在/E3002 filter格式）｜40xx 工作区｜50xx 网络（E5001超时/E5002证书）
- **08 注意事项**：⚠️已关闭执行不在执行列表中（需ID断层探测）｜⚠️任务列表默认隐藏已关闭任务（--status默认unclosed，搜任务务必--status=all）｜⚠️无全局搜索（需逐个执行搜）｜browseType常用值｜多账号切换/--useEnv/--config｜--filter逗号=AND、多个--filter=OR｜自签证书E5002→insecure｜凭证安全红线

## 加载规则

- **简单查询**（看产品/项目/Bug 列表、看详情）：直接执行，无需读文档。
- **不确定模块或参数**：先 `zentao <module> help` 或 `zentao <module> <action> help`；仍不确定再读 02。
- **筛选/搜索/分页/JSON 处理**：读 03。
- **AI 编排流程 / 意图映射**：读 05。
- **命令报错**：读 07 按错误码处理。
- **执行/任务查询、搜索、状态筛选，或查不到数据**：读 08（重点看⚠️条目，避免踩坑）。
- **需要现成命令模板**：读 06。
- **配置默认值 / 批量操作 / MCP 集成**：读 04。
