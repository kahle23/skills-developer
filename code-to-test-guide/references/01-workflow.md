# 01 - 工作流：从代码到测试

## 6 步流程

```
收集输入 → 读代码（测试视角）→ 提取测试点 → 设计用例 → 生成脚本 → 标注待补充
```

### 第 1 步：收集输入

| 输入类型 | 说明 | 必需性 |
|---------|------|--------|
| 代码路径（前端/后端） | 哪个模块、哪些目录 | ⚠️必需，至少有其一 |
| 截图 | 页面 UI 截图，帮助理解表单结构和交互 | 推荐，加速用例设计 |
| 口头描述 | 功能是干什么的、谁用、什么场景 | 推荐，加速理解 |
| 系统访问地址 | 测试环境的 URL、登录账号 | ⚠️生成脚本时必需 |
| 数据库访问 | 能看表结构和数据，用于断言验证 | 可选，有则断言更准 |

> **草台班子场景的特殊处理**：用户可能只甩来一句"帮我给 XX 功能写测试"加一堆代码路径，没有截图也没有测试环境地址。这时**不要追问一堆**，先读代码生成用例和脚本，把缺失的环境信息标注为待补充。

### 第 2 步：读代码（测试视角）

按 04-代码阅读策略 进行。核心原则：

- **前端找测试点**：表单字段、校验规则（required/pattern/validator）、按钮状态（disabled 条件）、交互行为（哪些操作触发接口）
- **后端找校验逻辑**：Controller 入参校验、Service 业务校验、状态流转条件
- **前后端对应**：前端表单字段 → 后端接口参数 → 数据库字段，三者对应起来就是断言依据

### 第 3 步：提取测试点

读代码后，列出所有可测试的点：

| 测试点类型 | 怎么提取 | 例子 |
|-----------|---------|------|
| 表单必填校验 | 前端 `rules` 的 `required: true` | "用户名"为空时提交应报错 |
| 格式校验 | 前端 `pattern` 或自定义 `validator` | 手机号格式不对应报错 |
| 业务校验 | 后端 Service 的 if/throw | 重复编码应提示"编码已存在" |
| 正常流程 | 主流程调用链 | 填完所有字段提交应成功 |
| 状态流转 | 后端 status 字段的变更 | 提交后状态变为"待审核" |
| 权限控制 | `@PreAuthorize` / 按钮 disabled | 无权限应看不到按钮 |
| 边界值 | 字段长度限制、数值范围 | 名称超过 50 字应报错 |
| 交互联动 | 级联选择、联动表单 | 选了部门后岗位列表应刷新 |

### 第 4 步：设计测试用例

按 02-测试用例模板 输出。关键原则：

- **每个测试点至少一个用例**
- **正常路径 + 异常路径 + 边界值都要覆盖**
- **预期结果必须可验证**（能看到的东西：提示消息、页面跳转、数据变化、按钮状态）

### 第 5 步：生成 Playwright 脚本

按 03-脚本模板 和 05-交互操作手册 生成。关键原则：

- **脚本和用例一一对应**：用例编号写在测试函数的 docstring 里
- **用 POM（页对象模式）**：页面元素定位和操作封装成页类，不散落在测试里
- **操作细节查 05**：下拉怎么选、日期怎么填、文件怎么传，全在交互手册里

### 第 6 步：标注待补充项

输出时，不确定或代码读不出来的内容用统一格式标注：

```markdown
<!-- 待补充：测试环境地址 -->
[待补充：测试环境 URL]

<!-- 待确认：不确定这个字段是否有唯一性校验 -->
[待确认：编码字段是否做了后端唯一性校验]
```

脚本中用 `# TODO` 标注：

```python
# TODO: 替换为实际测试环境地址
BASE_URL = "http://localhost:8080"

# TODO: 确认下拉框的选项值，代码中为动态加载
page.get_by_label("部门").click()
page.get_by_text("[待确认：部门选项]").click()
```

末尾汇总一个"待补充清单"，方便用户逐项处理。

---

## 草台班子场景速查

| 情况 | 处理 |
|------|------|
| 只有代码路径，没有截图和环境 | 先读代码生成用例和脚本框架，环境信息和截图标注为待补充 |
| 口头描述和代码冲突 | ⚠️**以代码为准**，但注明"口头描述为 XX，代码实际为 YY，请确认" |
| 只有前端代码 | 从前端表单和 API 调用生成用例，后端校验逻辑标注"需要后端代码补充" |
| 只有后端代码 | 从 Controller 和 Service 生成接口测试用例，UI 操作步骤标注"需要前端代码或截图补充" |
| 功能很大，代码很多 | 先列测试用例大纲（测试点清单），确认后再逐个细化 |
| 表单字段太多 | 先覆盖核心字段和所有校验规则，次要字段用参数化批量测 |

---

## 输出前的自检清单

- [ ] 测试用例覆盖了正常路径吗
- [ ] 测试用例覆盖了异常路径吗（校验失败、权限不足、重复提交）
- [ ] 测试用例覆盖了边界值吗（空值、最大长度、临界数值）
- [ ] 每个用例的预期结果都可验证吗（不是"应该成功"，而是"提示'新增成功'"）
- [ ] 脚本的测试函数和用例编号对应吗
- [ ] 元素定位用的是推荐方式吗（get_by_label / get_by_role 优先）
- [ ] 交互操作正确吗（原生 select 用 select_option，UI 组件库下拉用 click + click）
- [ ] 不确定的地方都标注了吗
- [ ] 末尾有待补充清单吗
