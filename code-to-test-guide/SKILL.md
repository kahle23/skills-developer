---
name: code-to-test-guide
description: 根据前后端代码逆向生成测试用例与 Python(Playwright) 自动化测试脚本。触发：生成测试用例、写自动化测试、E2E测试、UI自动化测试、补测试、浏览器自动化测试、表单测试、Playwright测试脚本、从代码生成测试。
---

# 代码转测试指南

从**已有前后端代码**逆向生成**测试用例 + Python(Playwright) 自动化测试脚本**。专为"功能开发完了、测试严重缺失"的团队设计。

一份代码，同时产出两类交付物：

- **测试用例文档**（给人看）：结构化的测试用例表格，含编号、前置条件、步骤、预期结果、优先级
- **自动化测试脚本**（给机器跑）：基于 Playwright + pytest 的 Python E2E 脚本，可直接 `pytest` 运行

## 能解决什么问题

> 测试靠手点、没有回归测试、改了代码不敢发布。本技能解决"代码写完了，测试从零开始"的痛点。

核心思路：**代码即真相**。从代码中提取表单字段、校验规则、业务流程、状态流转，自动生成覆盖正常/异常/边界的测试用例和对应脚本。

## 浏览器能操作什么

> Playwright 可以操作浏览器中的一切用户可见元素。以下场景全覆盖：

| 操作类型 | 能不能测 | 说明 |
|---------|---------|------|
| 文本输入 | ✅ | input / textarea / 富文本 |
| 下拉选择（原生） | ✅ | `<select>` 用 `select_option` |
| 下拉选择（UI 组件库） | ✅ | Element UI / Element Plus 的自定义下拉，点击触发 + 点选项 |
| 日期/时间选择 | ✅ | 日期范围、时间选择器，需处理弹出的日历面板 |
| 文件上传 | ✅ | 单文件、多文件 |
| 表格操作 | ✅ | 分页、排序、行内编辑、行内按钮 |
| 弹窗/对话框 | ✅ | 确认弹窗、消息提示、抽屉、Modal |
| 复选框/单选/开关 | ✅ | checkbox / radio / switch |
| 表单校验提示 | ✅ | 断言校验错误消息是否出现 |
| 登录态保持 | ✅ | storage state 复用 |
| 多标签页/iframe | ✅ | 弹出窗口、嵌入页面 |

## 工作流

1. 收集输入（代码路径/截图/口头描述）→ 2. 读代码（测试视角）→ 3. 提取测试点 → 4. 设计测试用例 → 5. 生成 Playwright 脚本 → 6. 标注待补充项

详见 01。

## 文档索引

| 编号 | 主题 | 关键条目 |
|-----|------|---------|
| 01 | 工作流 | 6 步流程 ｜ 收集输入清单 ｜ ⚠️代码阅读顺序（测试视角）｜ 提取测试点 ｜ 识别缺口主动询问 ｜ 待补充项标注法 |
| 02 | 测试用例模板 | 用例编号规则 ｜ 覆盖维度（正常/异常/边界）｜ 前置条件 ｜ 操作步骤 ｜ ⚠️预期结果必须可验证 ｜ 优先级判定 |
| 03 | Playwright 脚本模板 | ⚠️项目骨架（conftest/fixture/配置）｜ 目录结构 ｜ 依赖安装 ｜ POM 基础页类 ｜ 同步 API 写法 |
| 04 | 代码阅读策略（测试视角） | 前端找表单字段/校验规则/按钮状态 ｜ 后端找校验逻辑/状态流转 ｜ ⚠️前后端接口对应（断言依据）｜ 前端框架自动检测（Vue/React/jQuery） |
| 05 | 交互操作手册 | ⚠️文本输入 ｜ ⚠️下拉选择（原生 vs Element UI/Plus）｜ ⚠️日期选择 ｜ 文件上传 ｜ 表格操作 ｜ 弹窗确认 ｜ 表单校验断言 ｜ 等待策略 ｜ 元素定位优先级 |
| 99 | 完整示例 | 用户新增功能 ｜ 同时产出测试用例表 + 脚本 ｜ 登录 fixture ｜ POM 页对象 ｜ 完整可运行 |

## 加载规则

| 用户意图 | 读取 |
|---------|------|
| 生成测试用例 + 脚本（完整流程） | 01 → 02 → 03，交互细节查 05 |
| 只生成测试用例文档 | 01 → 02 |
| 只生成自动化脚本 | 01 → 03，操作细节查 05 |
| 不知道怎么测某个组件 / 交互卡住 | 05 |
| 不确定代码怎么读 / 怎么找测试点 | 04 |
| 前端框架不确定（Vue? React?） | 04 的「前端框架自动检测」 |
| 看完整范例对照写法 | 99 |

## 关联技能

- `code-to-doc-guide` — 代码转**文档**（技术文档 + 用户文档）。本技能是代码转**测试**，同属"代码逆向"系列，互补使用。
- `python-coding-guide` — Python 编码规范，生成测试脚本时参考 03(命名/结构) 和 08(pytest/fixture)。
- `feature-design-workflow` — 功能设计文档（开发前写）。设计文档里的接口和业务逻辑是测试用例的优质输入。
